# Optional — portal defaults: dev → http://localhost:8787, prod build → https://api.aliasist.tech
# Copy to `.env.local` if you need to override (e.g. a staging Worker URL).
# VITE_API_BASE=https://api.aliasist.tech
